function preload(){

}

function setup(){
  canvas = createCanvas(500, 400);
  canvas.center()
  video = createCapture(VIDEO);
  //video.position()
  //this to to set position of video
}

function draw(){
  background('white')
}